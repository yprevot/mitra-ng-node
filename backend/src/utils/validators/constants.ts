const NAME =  Object.freeze({
    min: 3,
    max: 100
});
const DESCRIPTION =  Object.freeze({
    min: 5,
    max: 1000
});
const PRICE =  Object.freeze({
    min: 1,
    max: 20000,
    precision: 2
});

export {
    NAME,
    DESCRIPTION,
    PRICE,
}
